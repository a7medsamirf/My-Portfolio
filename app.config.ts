export default defineAppConfig({
    ui: {
      strategy: "merge",
      button: {
        default: {
          size: 'md',
          color: 'gray',
          variant: 'ghost'
        }
      }
    }
  })
